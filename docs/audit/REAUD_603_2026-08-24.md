# REAUD-603 — INDEPENDENT RE-AUDIT REPORT (2026-08-24)

**Method:** fresh-agent audit with zero implementation context; read-only.
Auditor sampled every major remediation claim against actual code and hunted
for regressions introduced by the fixes themselves.

## Verdict at arrival: FAIL — exactly why G7 exists

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | **CRITICAL** | Outbound idempotency hard gate never existed: no unique index on `message_outbox.idempotency_key`; reply keys text-based (`text[:40]`); Meta retry during sync draft ⇒ duplicate replies (the original prod-proven ×3 failure) | **FIXED** — partial `ux_outbox_idem` (+dedupe migration), reply keys anchored to inbound wamid (`out:{kind}:{lead}:{wamid}`), `enqueue()` insert-or-return-existing via ON CONFLICT matching the partial index |
| 2 | HIGH | Price-intent path called allow() but never record() → unbounded paid LLM calls | **FIXED** — quote path now gated AND charged |
| 3 | HIGH | Telegram bot token leakable via transport exception strings into journald/alert text | **FIXED** — exceptions reduced to `{type}: details suppressed` everywhere |
| 4 | HIGH | Console /chat /offers bypassed consent/valve/template — G6 "initiations disabled" claim false | **FIXED** — inbox_send_message(initiation=True) enforces ConsentGate + 24h window-or-template + valve reservation + initiation tag; console routes through it with 🛡️ refusals |
| 5 | MEDIUM | Learning pipeline dead in prod (NameError swallowed: undefined governor/wa_id) | **FIXED** — governor hoisted into runtime; thread runs governed allow→record |
| 6 | MEDIUM | Delivery receipts polluted LOCAL send state (C3 deviation) | **FIXED** — new `delivery_status` column; recorder writes there; STATUSES legal set restored |
| 7 | MEDIUM | Stale reclaim blind-requeued crash-window rows | **FIXED** — no-pmid stale rows → `uncertain` state, excluded from claims, owner-visible log (MANUAL_ONLY per §9) |
| 8 | MEDIUM | No outbox drainer outside request threads | **FIXED** — `outbox.drain` job every minute (registry builds self-contained worker) |
| 9 | MEDIUM | load_env mutates os.environ for any caller (the defect class behind the WABA incident) | **FIXED** — `mutate_environ=False` param; test callers migrated |
| 10-13 | LOW | /chat UnboundLocalError (R5) · won_opportunity multi-commit (D6) · classifier pre-negation edge · attempts-not-incremented-on-claim | **ACCEPTED RESIDUALS** — ticketed P3; each bounded (handover double-gating, owner-only surface, WAL single-writer) |

## Spot-check results (independent verification)
Atomic claims TRUE · C6 negation TRUE · C5 firewall TRUE-with-caveat ·
BAK-103 ENOSPC ordering TRUE · autocommit durability TRUE · storage boundary
TRUE · S-series auth TRUE · incident-chain guards TRUE ·
G1-idempotency & COST-all-sites claims were FALSE on arrival → fixed above.

## G7 outcome
- All CRITICAL/HIGH/MEDIUM findings closed same-day with tests
  (**569→570 suite**, includes idempotent-enqueue, uncertain-state,
  delivery-status updates).
- **Score: B** — gates G1–G6 hold under independent scrutiny after closure.
- **A/READY remains withheld** pending external items:
  1. Quiet-hours production re-measure (requires live traffic — blocked by ban)
  2. Live opt-in validation with a working number (blocked by ban)
  3. LOW-residuals triage at next cycle

## Process note
The auditor's sharpest line is adopted as standing policy:
*"hard gates must exist in the LIVE paths, not only in helper-class tests."*
Gate evidence going forward must cite call-site wiring, not unit fixtures alone.
