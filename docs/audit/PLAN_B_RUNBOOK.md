# PLAN B RUNBOOK — WABA REBUILD AFTER 2026-08-24 BAN

**Status:** Appeal rejected (integrity violation upheld). All sending remains
FROZEN until the activation checklist below completes with new credentials.

## القاعدة الذهبية
لا تنشئ الحساب الجديد تحت نفس مدير الأعمال المحظور. مساران آمنان فقط.

## القرار المطلوب منك (اختر مساراً)

### المسار A — Cloud API مباشر (مجاني، أبطأ)
1. أنشئ **Business Manager جديداً كلياً**: بريد إلكتروني لم يسبق استخدامه مع ميتا + هاتف تحقق جديد
2. أضف شركة (إن كانت الكيان التجاري نفسه محظوراً أيضاً في ميتا، فكّر بمسار B)
3. وثّق الشركة (Business Verification) — سيتطلب سجل تجاري/أوراق هوية
4. اشترِ/جهّز رقم هاتف جديد (لا تستخدم رقماً سبق إضافته لأي حساب محظور) — SMS/Voice للتحقق متاح
5. أنشئ WABA داخل الـBM الجديد وأضف الرقم
6. أنشئ App Developer جديد على developers.facebook.com واربطه
7. اجمع: `WHATSAPP_PHONE_NUMBER_ID` · `WHATSAPP_ACCESS_TOKEN` (دائم عبر System User) · `WHATSAPP_APP_SECRET` · `WHATSAPP_VERIFY_TOKEN` (اخترعه بنفسك)

### المسار B — مزود BSP ⭐ (مدفوع، أسرع وأأمن ارتباطاً)
- مزودات تعمل بنفس شكل Cloud API (توصيل مباشر بكودنا): **360dialog** · Wati · Rasayel
- Twilio ممكن لكنه API مختلف — سأبني طبقة تكييف (يوم عمل إضافي)
- الخطوات: تسجيل عند المزود → توثيق → اختيار/شراء رقم → يستلم مفاتيح API منهم بنفس أسماء المتغيرات تقريباً
- ميزة إضافية: فلاتر الجودة والمراجعة تديرها هم غالباً

## ما أحتاجه منك للتفعيل (5 دقائق بعد حصولك على المفاتيح)
أرسل لي القيم الجديدة وسأنفذ:
1. تحديث `.env` (المفاتيح الخمسة أعلاه + `OWNER_ALERT_*` إن تغيرت)
2. `production.yaml`: إعادة `production_enabled: true`
3. restart الخدمتين + health + اختبار إرسال/استقبال حي على رقمك الشخصي
4. ضبط tier 0 والتقاط أول موافقة ضمنية أمامك

## تشيك ليست التشغيل الآمن (مفعّلة تقنياً بالفعل — لا تعكسها أبداً)
- [x] بوابة الموافقة: أول رسالة واردة = opt-in مسجل تلقائياً (`test_implied_consent`)
- [x] المبادرات فقط عبر قالب معتمد تضبطه في `configs/app.yaml → compliance.approved_templates`
- [x] سقف يومي متدرج 50→250→1000 (ارفع tier يدوياً بعد أسبوع نظيف لكل درجة)
- [x] `/approve <n>` من تيليجرام لرفع سقف اليوم استثناءً
- [ ] **عليك:** قبل أي حمل خارجي — جهّز قالباً واحداً على الأقل واعتمده في ميتا ثم أضفه للتهيئة

## سياسة العملاء الحاليين
CRM فيه عملاؤك الحقيقيون (نظّفنا الوهميين). قواعد التواصل معهم على الرقم الجديد:
- من يراسلك أولاً → يُسجل consent تلقائياً → ردود AI طبيعية فوراً
- المتابعات الصادرة → فقط لمن لديه `consent_at` + قالب معتمد + ضمن السقوف
- لا استيراد جماعي ولا رسائل "تفعيل حسابنا الجديد" جماعية — هذا يعيد الدورة

## Rollback
كل شيء يبقى مجمداً (`production_enabled: false` + scheduler stopped) حتى اكتمال التشيك ليست. عودة للحالة الحالية = عكس خطوتي 1-2.
